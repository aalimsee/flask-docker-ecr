

name: Docker CI - Build & Push to ECR

on:
  push:
    branches: [main]
    paths:
      - '**/Dockerfile'
      - '.github/workflows/docker-build.yml'

env:
  PROJECT_NAME: flask-docker-ecr
  AWS_REGION: us-east-1

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (Login to DockerHub not necessary because of ECR used)
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR (Set up Docker Buildx not necessary because of AWS ECR)
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image to ECR
        run: |
          IMAGE_TAG=${{ github.sha }}
          ECR_URI=$(aws ecr describe-repositories --repository-names ${PROJECT_NAME}-repo --query "repositories[0].repositoryUri" --output text)
          docker build -t $ECR_URI:$IMAGE_TAG .
          docker push $ECR_URI:$IMAGE_TAG

          